// The initial snippet lives in example.rzk (a plain Rzk file, typechecked by
// the "Check Rzk formalizations" CI job) and is bundled here as raw text.
import example from './example.rzk?raw'

export { example }
